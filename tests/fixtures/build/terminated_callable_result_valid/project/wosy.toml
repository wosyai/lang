[package]
name = "terminated_callable_result_valid"
default_target = "app"

[comments]
categories = []

[builders.native]
command = ["scripts/build-native"]
args = []
toolchain_identity = "llvm-18.1.3"

[targets.app]
root = "src/main.w"

[targets.app.artifacts.main]
artifact = "Main"

[targets.app.artifacts.main.profiles.dev]
backend = "llvm"
output = "native"
builder = "native"
run_runner = "native"

[runners.native]
command = ["scripts/run-native"]
args = []

[targets.app.profiles.dev]
main_artifact = "main"
