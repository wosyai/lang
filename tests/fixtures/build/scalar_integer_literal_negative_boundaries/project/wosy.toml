[package]
name = "scalar_integer_literal_negative_boundaries"
default_target = "app"

[comments]
categories = []

[builders.native]
command = ["python3", "scripts/build-native"]
args = []
toolchain_identity = "llvm-18.1.3"

[runners.native]
command = ["python3", "scripts/run-native"]
args = []

[targets.app]
root = "src/main.w"

[targets.app.artifacts.main]
artifact = "Main"

[targets.app.artifacts.main.profiles.dev]
backend = "llvm"
output = "native"
builder = "native"
run_runner = "native"

[targets.app.profiles.dev]
main_artifact = "main"
