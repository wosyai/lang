#!/usr/bin/env python3
import subprocess
import sys

partition, output_dir, manifest = sys.argv[1:]
subprocess.run(["/usr/bin/clang", "-x", "ir", partition, "-no-pie", "-Wl,--unresolved-symbols=ignore-all", "-o", output_dir + "/main"], check=True)
