[package]
name = "unused_binding_invalid"
default_target = "app"

[comments]
categories = []

[targets.app]
root = "src/main.w"

[targets.app.artifacts.main]
artifact = "Main"

[targets.app.artifacts.main.profiles.dev]
backend = "llvm"
output = "native"
builder = "native"
run_runner = "native"

[builders.native]
command = ["scripts/build-native"]
args = []
toolchain_identity = "llvm-18.1.3"

[runners.native]
command = ["scripts/run-native"]
args = []

[targets.app.profiles.dev]
main_artifact = "main"
