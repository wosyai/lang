[package]
name = "extern_symbol_collisions"
default_target = "app"

[comments]
categories = []

[builders.native]
command = ["sh", "scripts/build-native"]
args = []

[runners.native]
command = ["sh", "scripts/run-native"]
args = []

[targets.app]
root = "src/main.w"

[targets.app.artifacts.main]
artifact = "Main"

[targets.app.artifacts.main.profiles.dev]
backend = "llvm"
output = "native"
builder = "native"
run_runner = "native"

[targets.app.profiles.dev]
main_artifact = "main"
