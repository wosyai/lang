[package]
name = "scalar_base_invalid_name"
default_target = "app"

[comments]
categories = []

[builders.native]
command = ["python", "scripts/build-native"]
args = []
toolchain_identity = "llvm-18.1.3"

[runners.native]
command = ["scripts/run-native"]
args = []

[targets.app]
root = "src/main.w"

[targets.app.artifacts.main]
artifact = "Main"

[targets.app.artifacts.main.profiles.dev]
backend = "llvm"
output = "native"
runtime = "native64"
builder = "native"
run_runner = "native"

[targets.app.profiles.dev]
main_artifact = "main"
