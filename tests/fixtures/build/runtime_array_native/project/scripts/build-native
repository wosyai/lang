#!/usr/bin/env python3
import subprocess
import sys

partition, output_dir, manifest = sys.argv[1:]
subprocess.run(
    ["clang", "-x", "ir", partition, "runtime/native64.ll", "-o", output_dir + "/main"],
    check=True,
)
