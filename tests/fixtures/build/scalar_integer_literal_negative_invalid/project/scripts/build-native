#!/usr/bin/env python3
import subprocess
import sys

partition, output_dir, manifest, runtime = sys.argv[1:]
subprocess.run(["/usr/bin/clang", "-x", "ir", partition, runtime, "-o", output_dir + "/main"], check=True)
