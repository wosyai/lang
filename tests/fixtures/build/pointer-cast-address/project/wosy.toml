[package]
name = "pointer-cast-address"
default_target = "app"

[dependencies.std]
path = "stdlib"

[comments]
categories = []

[builders.llvm-wasm]
command = ["python", "scripts/build-wasm"]
args = []
toolchain_identity = "llvm-18.1.4"

[runners.wasmtime-wasip1]
command = ["python", "scripts/run-wasmtime"]
args = []

[targets.app]
root = "src/main.w"

[targets.app.artifacts.main]
artifact = "PointerCastAddress"

[targets.app.artifacts.main.profiles.dev]
backend = "llvm"
output = "wasm-wasip1"
builder = "llvm-wasm"
run_runner = "wasmtime-wasip1"

[targets.app.profiles.dev]
main_artifact = "main"
