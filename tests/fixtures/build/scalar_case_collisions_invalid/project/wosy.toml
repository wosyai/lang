[package]
name = "scalar_case_collisions_invalid"
default_target = "app"

[comments]
categories = []

[targets.app]
root = "src/main.w"

[targets.app.artifacts.main]
artifact = "Main"

[targets.app.artifacts.main.profiles.dev]
backend = "llvm"
output = "native"
runtime = "native64"
builder = "native"
run_runner = "native"

[targets.app.profiles.dev]
main_artifact = "main"

[builders.native]
command = ["sh", "scripts/build-native"]
args = []
toolchain_identity = "llvm-18.1.3"

[runners.native]
command = ["sh", "scripts/run-native"]
args = []
